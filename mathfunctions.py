# Here math is a module and all the methods in math module are declared as static method so that we can access
# them directly with class name without creating an object.

import math
print(math.ceil(2.9))
print(math.floor(2.9))